# AWS credentials (IAM user with SES send + SQS receive permissions)
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=us-east-1

# SQS queue URL (receives SES events via SNS)
SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/123456789012/ghost-ses-events

# SES Configuration Set name (must match the one attached to your SES identity)
SES_CONFIGURATION_SET=ghost-ses-proxy

# API key that Ghost will use to authenticate (set this to any secure random string)
PROXY_API_KEY=your-secure-api-key-here

# Your domain (must match the domain configured in Ghost's Mailgun settings)
MAILGUN_DOMAIN=example.com

# Optional: port to listen on (default: 3003)
# PORT=3003

# Optional: log level (default: info). One of trace|debug|info|warn|error|fatal.
# Gates every log call; an invalid value aborts startup.
# LOG_LEVEL=info

# Optional: max concurrent SES sends per batch (default: 10)
# SEND_CONCURRENCY=10

# Optional: SQLite database path (default: /data/ses-proxy.db)
# DB_PATH=/data/ses-proxy.db
