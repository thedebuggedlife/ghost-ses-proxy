{
  "name": "ghost-ses-proxy-cdk",
  "version": "1.0.0",
  "description": "AWS CDK app provisioning the infrastructure ghost-ses-proxy needs",
  "private": true,
  "type": "module",
  "scripts": {
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "generate-env": "tsx scripts/generate-proxy-env.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-cloudformation": "^3.700.0",
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "aws-cdk-lib": "^2",
    "constructs": "^10",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "aws-cdk": "^2",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^4.0.0"
  }
}
